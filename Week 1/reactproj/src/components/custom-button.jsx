import React from "react"
const CustomButton = ({props, onClick})=>{

    /*const obj={
        id:1,
        name:'sskha' 
    };

    const {id} = obj*/

    return <button onClick={onClick}>{value}</button>
}

export default CustomButton